{
  "name": "inicio-red-bo",
  "version": "1.0.0",
  "description": "inicio.red.bo — Directorio digital curado para Bolivia y Latinoamérica",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute redbo-main --file=db/schema.sql",
    "db:seed": "wrangler d1 execute redbo-main --file=db/seeds.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
